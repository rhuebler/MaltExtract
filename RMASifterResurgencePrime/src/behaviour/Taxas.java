package behaviour;
/**
 * Enum that controls which tax IDs shall be used essentially use all IDs in the file 
 * which is often a waste of resources except for Filter.SCAN and Filter.CRAWL
 * 
 * @author huebler
 *
 */
public enum Taxas {
	ALL, USER
}
